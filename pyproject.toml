[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "search-service"
version = "0.1.0"
description = "Search and indexing microservice"
requires-python = ">=3.9"
dependencies = ["core-logger"]

[tool.setuptools.packages.find]
where = ["src"]
