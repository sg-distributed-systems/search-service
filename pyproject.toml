[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "search-service"
version = "2.0.0"
description = "Search and indexing microservice"
requires-python = ">=3.9"
dependencies = ["core-logger", "fastapi>=0.110", "uvicorn[standard]>=0.27"]

[tool.setuptools.packages.find]
where = ["src"]
